const Tag = ({ text }) => {
  return (
    <span className="inline-block px-3 py-1 bg-blue-100 text-blue-800 rounded-full text-sm font-medium mr-2 mb-2">
      {text}
    </span>
  );
};

export default Tag;

